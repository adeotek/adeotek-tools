name: Build and Push homelab-chatbot Docker Image

on:
  workflow_dispatch:
    inputs:
      version_tag:
        description: "Version tag for the Docker image (e.g. 0.1.0)"
        required: false
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker tags
        id: docker_tags
        env:
          VERSION_TAG: ${{ github.event.inputs.version_tag }}
        run: |
          TAGS="ghcr.io/adeotek/homelab-chatbot:latest"
          if [ -n "$VERSION_TAG" ]; then
            TAGS="$TAGS,ghcr.io/adeotek/homelab-chatbot:$VERSION_TAG"
          fi
          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: ./homelab-chatbot
          file: ./homelab-chatbot/Dockerfile
          push: true
          tags: ${{ steps.docker_tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
